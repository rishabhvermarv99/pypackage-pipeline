name: Python package installation for windows

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  run:

    runs-on: windows-latest

    steps:

    - name: Install package
      run: pip install sigmoidpythonlib    

    - name: Run tests
      run: python test_myfunction.py   
