name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: macOS-latest

    steps:

    - name: Install package
      run: pip install sigmoidpythonlib    

   
